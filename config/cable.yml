# developmentのasyncアダプタは同一プロセス内でのみ動作する。
# コンソールから画面へ反映を確認したい場合はWeb Consoleを使う。
development:
  adapter: async

test:
  adapter: test

production:
  adapter: solid_cable
  connects_to:
    database:
      writing: cable
  polling_interval: 0.1.seconds
  message_retention: 1.day
